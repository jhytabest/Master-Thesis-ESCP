# Mapping Job

Workflow A: build and freeze mapping bundles.

## Entrypoints

```
propose_mappings --version_id <id> --column COMPANY_STATUS
freeze_mapping_bundle --mapping_bundle_path <path> --output_path <path>
```

