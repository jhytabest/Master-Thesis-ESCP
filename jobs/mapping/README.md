# Mapping Job

Workflow A: build and freeze mapping bundles.

## Entrypoints

```
propose_mappings --version_id <id> [--output_prefix <r2/prefix>] [--model <gemini-model>]
freeze_mapping_bundle --proposal_prefix <r2/prefix> --output_bundle_id <id>
```
