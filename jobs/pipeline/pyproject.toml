[project]
name = "pipeline-job"
version = "0.1.0"
requires-python = ">=3.11"
dependencies = [
  "boto3>=1.34.0",
  "pandas>=2.2.0",
  "pyarrow>=14.0.0",
  "scikit-learn>=1.4.0"
]

[project.scripts]
pipeline = "pipeline.cli:main"
